# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index
GET    /explore                     controllers.HomeController.explore
GET    /books/:categoryId           controllers.HomeController.books(categoryId: Long)

# Admin
GET     /import/books               controllers.SystemAdmin.importBooks

# Secret Admin
GET     /test/9uy5                  controllers.SecretAdmin.test
GET     /populate/books/j8r3        controllers.SecretAdmin.populateBooks

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
