package utils;

import models.User;
import play.mvc.Controller;
import play.mvc.Http;

public class SessionUtil extends Controller {
    /*public static void create(User user, Http.Request request, Http.Response response) {
        session("email", user.email);
        session("userId", String.valueOf(user.id));
    }*/
}
